# Зависимости, которых нет ни у Agent_1, ни у hw9 — только оценка (hw10).
#
# Почему отдельный файл, а не строки в requirements.txt: тот приезжает по
# цепочке Agent_1 → hw9 → hw10 и перезаписывается целиком. Дописанное туда
# исчезло бы при первой же синхронизации, причём молча. Ровно эта история уже
# случилась в hw9 с протоколами — см. requirements-protocols.txt.
#
#   uv pip install -r requirements.txt -r requirements-protocols.txt \
#                  -r requirements-eval.txt
#
# ПОЧЕМУ У hw10 СВОЯ .venv, А НЕ СИМЛИНК НА hw9
#
# deepeval не добавляется к окружению hw9, а МЕНЯЕТ его: тянет за собой
# click 8.3.3 вместо 8.4.2 и rich 14.3.4 вместо 15.0.0, то есть откатывает оба.
# На них в hw9 живут uvicorn и mcp, поднимающие SearchMCP/ReportMCP/ACP. Цена
# ошибки несимметрична: измерительный инструмент, сломавший измеряемую систему,
# ломает её тихо и не в том месте, где будешь искать. Полтора гигабайта диска
# дешевле такого вечера.

# LLM-as-a-judge + pytest-обвязка (pytest, xdist, rerunfailures приезжают с ним).
deepeval==4.1.10
