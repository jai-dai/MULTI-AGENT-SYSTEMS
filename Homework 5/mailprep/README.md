# mailprep — препроцессор почты для локального RAG

Драфт под стек: **Gmail API → mailprep → bge-m3 (Ollama) → Qdrant → MCP**

Дорабатывать в VSCode вместе с Claude Code.

---

## Структура

```
mailprep/
├── models.py           # датаклассы: RawMessage → CleanMessage → Thread → Chunk
├── clean.py            # ★ ЯДРО: обрезка цитат/подписей/дисклеймеров + detect_lang
├── pipeline.py         # склейка цепочек, чанкинг, payload для Qdrant
└── inspect_quality.py  # ★ инструмент отладки паттернов очистки
```

Два файла со звёздочкой — то, где сосредоточена вся ценность.
Остальное — обвязка.

---

## Догрузка новых писем — инкрементальная

```bash
.venv/bin/python -m mailprep.imap_fetch          # добрать только новое
.venv/bin/python -m mailprep.imap_fetch --stats  # что уже лежит в базе
```

В `mail.db` есть таблица `sync` с `last_uid` по каждой папке, и запрос идёт как
`UID last_uid+1:*` — заново скачивается только то, чего в базе нет. Отдельно
проверяется `UIDVALIDITY`: если Gmail её сменил, папка перечитывается с нуля,
потому что старый счётчик после этого указывает на чужие письма.

Что важно помнить дальше по конвейеру: **база — снимок**. Всё, что видит агент,
это результат последней выгрузки, а не живой ящик. Поэтому `list_mail` говорит
об этом прямо в своём ответе, а не оставляет модели догадываться.

## Быстрый старт

```bash
# 1. Выгрузить письма в JSON (свой скрипт на Gmail API)
python dump_gmail.py > dump.json

# 2. Посмотреть, как отрабатывает очистка
python -m mailprep.inspect_quality dump.json -v

# 3. Разобрать конкретное письмо
python -m mailprep.inspect_quality dump.json --id 18f2a3b4c5

# 4. Дополнить паттерны в clean.py, повторить шаг 2
```

## Как читать отчёт inspect_quality

| Метрика | Что означает |
|---|---|
| `пустые после очистки` высокий % | Паттерны **слишком жадные** — режут полезный текст |
| `почти не тронуто` высокий % | Паттернов **не хватает** — цитаты/подписи остаются в индексе |
| `перечистили` (остаток <15%) | Смотреть глазами: где эвристика ошиблась |
| `недочистили` (остаток >95%) | Смотреть хвост: какой паттерн подписи не покрыт |

Цель — довести оба крайних показателя до минимума на **своих** данных.
Универсальных паттернов не существует, у каждой переписки свой мусор.

---

## Ключевые решения (и почему)

**1 письмо = 1 чанк.** Резать письмо по 512 токенов бессмысленно — оно и так
короткое, а разрыв контекста хуже, чем длинный чанк. Длинные письма
(>4000 симв.) режутся по абзацам с overlap 300.

**Контекстная шапка перед эмбеддингом.** Каждый чанк начинается с
`Тема: ... | От: ... | Дата: ...`. Без этого письмо «да, согласны, 14 недель»
не находится ничем. Это главный приём для качества RAG по почте.

**Порядок очистки важен.** noise → quote header → quoted lines → disclaimer →
signature. Подпись режется последней и только в последних 15 строках —
иначе слово «Regards» в середине текста убьёт полписьма.

**detect_lang без внешних зависимостей.** Различает ru/uk по характерным
буквам (і/ї/є/ґ vs ы/ъ/э/ё). bge-m3 мультиязычный, так что на эмбеддинг
не влияет — нужно для фильтров и отладки.

---

## Что доработать (по порядку)

### 1. Паттерны под свои данные — обязательно
В `clean.py` расширить: `QUOTE_HEADERS`, `SIGNATURE_MARKERS`,
`DISCLAIMER_STARTS`, `NOISE_LINES`. Особенно корейские варианты, если
переписка с Doosan / GS / Woojin пойдёт активно.

### 2. Ридер Gmail
Написать `dump_gmail.py` на `google-api-python-client`. Отдавать JSON в
формате, который ждёт `load_dump()` (описан в докстринге).
Scope: `gmail.readonly`.

### 3. Эмбеддинги
```python
import httpx
def embed(texts: list[str]) -> list[list[float]]:
    r = httpx.post("http://localhost:11434/api/embed",
                   json={"model": "bge-m3", "input": texts}, timeout=120)
    return r.json()["embeddings"]
```

### 4. Qdrant + гибридный поиск
bge-m3 умеет dense + sparse одновременно. Через Ollama обычно доступен
только dense — для sparse либо поднять FlagEmbedding напрямую, либо
взять BM25 отдельно и слить результаты (RRF). Для деловой почты
гибрид критичен: точные совпадения (имена, номера `ATON-DE/2026-001`,
домены) векторный поиск размывает.

Индексы в Qdrant поставить на: `thread_domains`, `lang`, `date`,
`has_attachments` — это основные фильтры.

### 5. MCP-сервер
Два инструмента достаточно:
- `search_mail(query, domain?, date_from?, date_to?, limit?)` → список чанков
- `get_thread(thread_id)` → полная цепочка

SDK: `pip install mcp`.

---

## Идеи на потом

- **Дедупликация**: одно и то же письмо часто приходит и в INBOX, и в SENT.
  Хэш по (нормализованная тема + дата + первые 200 симв.).
- **Извлечение сущностей**: номера документов, суммы, даты, Incoterms —
  в payload отдельными полями, чтобы фильтровать точно.
- **Приоритет свежести**: при равной релевантности поднимать свежие письма.

---

## Вложения проиндексированы (сделано)

Тело письма в деловой переписке часто пустое по смыслу: «договор у вкладенні»,
«КП додаю». Индекс писем такое письмо честно находит и ничего не отвечает —
содержание лежит в файле рядом.

Вложения **не требуют своего конвейера**: `imap_fetch` кладёт их на диск в
`mail/attachments/<message-id>/`, а дальше это обычные документы, которые
забирает обычный `ingest.py`:

```bash
DATA_DIR=mail/attachments INDEX_DIR=index_vl caffeinate -is \
  .venv/bin/python -u ingest.py 2>&1 | tee att_ingest.log
```

Именно `index_vl`, а не `index_mail`. Причина техническая: у почтового индекса
свой манифест от mailprep, и `ingest.py` его не дополнит, а перестроит с нуля,
затерев письма. В `index_vl` та же bge-m3 и та же подпись эмбеддингов, поэтому
это чистое инкрементальное добавление — 336 файлов исходного корпуса остаются
на месте (удалённым считается только то, чего нет на диске).

Замер на реальном ящике, 94 вложения / 143 МБ / 34 минуты:

| | было | стало |
|---|---|---|
| файлов в индексе | 336 | 430 |
| чанков | 6409 | 7347 |

- **83 PDF + 11 DOCX, 293 страницы**, ни одного файла без текста;
- **OCR отработал на 33 файлах / 126 страницах** — уставы, паспорта, выписки
  ЕГР, сканы КП. Это ровно те документы, ради которых всё и делалось;
- **650 дубликатов отсеяно до эмбеддинга.** Один и тот же договор ходит по
  нескольким цепочкам и сохраняется под каждым message-id отдельно, так что
  дублей тут больше, чем в обычной папке с документами. Отсев *до* эмбеддинга
  сэкономил около 18 минут — дедупликация после него оплачивает векторы, чтобы
  тут же их выбросить.

### Метаданные письма на вложениях

Вложение — документ по содержимому и почтовый объект по происхождению. Пока в
чанке была только первая половина, спросить «що мені надсилав бухгалтер» было
нечем: фильтр `correspondent` смотрит на `sender_email`, а у PDF отправителя нет.

Связь восстанавливается по имени каталога — `imap_fetch` кладёт вложения в
`mail/attachments/<attachment_folder(message_id)>/`, и
[`attachments.py`](attachments.py) читает ту же функцию в обратную сторону.
Функция ровно одна на оба направления **намеренно**: разойдись формула на один
символ между тем, кто пишет каталог, и тем, кто его ищет, — вложения перестали
бы находить своё письмо, и ни одной ошибки при этом не возникло бы.

Имена полей участников (`sender_email`, `to_emails`, `cc_emails`) совпадают с
почтовыми чанками из `index.py` не для красоты: `retriever` фильтрует оба
индекса одним и тем же кодом, и расхождение имён дало бы фильтр, который молча
работает на одном индексе и не работает на другом.

А вот дата письма кладётся в `mail_date`, **не** в `date`, и это исправление
собственного промаха. Сначала она писалась в `date` — и устав 2020 года стал
выводиться как `2026-06-16`, потому что письмо с ним переслали в этом году.
«Когда документ создан» и «когда его прислали» — две разные величины, и вторая
не имеет права занимать место первой. Теперь в выдаче видно обе:

```
[Статут Атон Груп нова редакція 2020.pdf p.1] 2020-09-22 (document), sent 2026-06-16 OCR
```

Проставляются они **без переэмбеддинга**:

```bash
INDEX_DIR=index_vl .venv/bin/python ingest.py --relabel
# → chunks: 7347 of 7347 | files read: 381
# → date source: document 5897, filesystem 1387, mail 63
# → chunks with mail metadata: 938
```

Отдельная команда нужна потому, что индексация инкрементальна по digest файла:
файлы не изменились, обычный прогон честно пропустил бы все 94, и новые поля не
появились бы никогда. Текст договора не меняется от того, что мы теперь знаем
отправителя, — метаданные не входят в вектор, и платить за них 26 минут
эмбеддинга незачем. Чанки сохраняют свои `id` и порядок, растёт только payload.

Замер после: 40 каталогов на диске сопоставились с базой полностью, 0 промахов;
по адресу `buh.caris` стали достижимы 117 чанков, а запрос «документи статутні»
с этим фильтром возвращает `Cтатут.pdf` со скором 0.86 — раньше он не
возвращал ничего.
